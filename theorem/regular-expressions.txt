==================================================
📘 정규식(Regular Expression) 학습 노트
==================================================


==================================================
1. 정규식 개념 (WHY)
==================================================

■ 정규식이란?
- 문자열에서 특정 "패턴(규칙)"을 찾기 위한 문법
- 검사(test), 치환(replace), 추출(match)에 사용

기본 형태:
  /패턴/옵션


--------------------------------------------------
1-1. 정규식은 언제 사용하는가?
--------------------------------------------------

정규식은
"문자열이 일정한 규칙을 가져야 할 때" 사용한다.

- 사람이 눈으로 판단하던 규칙을
- 코드가 대신 판단해야 할 때

사용 예:
- 이메일 / 비밀번호 형식 검사
- 전화번호 형식 검사
- 파일 확장자 검사
- 사용자 입력값 정제


--------------------------------------------------
1-2. 왜 정규식을 사용하는가?
--------------------------------------------------

정규식은 편해서가 아니라 "필요해서" 사용한다.

사용 이유:
1) 조건문으로 처리하면 코드가 너무 길어질 때
2) 문자열의 시작/끝 같은 위치 개념이 중요할 때
3) 규칙이 자주 바뀌거나 재사용해야 할 때

※ 정규식은
"문자열을 다루는 기술"이 아니라
"문자열의 규칙을 정의하는 언어"다.


--------------------------------------------------
1-3. 정규식을 쓰지 않아도 되는 경우
--------------------------------------------------

- 문자열이 완전히 고정된 경우 (=== 비교)
- 구조가 단순한 경우 (split, index 접근)

정규식은 항상 최선은 아니다.


==================================================
2. 정규식 기초 문법 (WHAT)
==================================================

--------------------------------------------------
2-1. 핵심 메타 문자
--------------------------------------------------

^   문자열 시작
$   문자열 끝

\d  숫자 (0~9)
\D  숫자가 아닌 문자

\w  문자 + 숫자 + _
\W  특수문자

\s  공백 (스페이스, 탭, 줄바꿈)
\S  공백이 아닌 문자


--------------------------------------------------
2-2. 문자 범위 []
--------------------------------------------------

[a-z]       소문자
[A-Z]       대문자
[a-zA-Z]    영문 전체
[0-9]       숫자
[가-힣]     한글


부정:
[^a-z]  → 소문자가 아닌 모든 문자


--------------------------------------------------
2-3. 반복 수량자
--------------------------------------------------

+   1번 이상
*   0번 이상
?   0 또는 1번

{n}     정확히 n번
{n,}    n번 이상
{n,m}   n~m번


--------------------------------------------------
2-4. OR 조건
--------------------------------------------------

(png|jpg|gif)
→ 셋 중 하나 허용


==================================================
3. 정규식 메서드 (HOW)
==================================================

--------------------------------------------------
3-1. test()  : 검사
--------------------------------------------------

- 문자열이 패턴을 만족하는지 검사
- 결과: true / false

사용 예:
  /\d/.test("abc123")  → true


--------------------------------------------------
3-2. replace() : 제거 / 변환
--------------------------------------------------

- 패턴에 맞는 문자열을 다른 문자열로 치환

사용 예:
  "010-1234".replace(/\D/g, "")
  → "0101234"


--------------------------------------------------
3-3. match() : 추출
--------------------------------------------------

- 패턴에 맞는 문자열을 배열로 반환

사용 예:
  "abc123".match(/\d/g)
  → ["1","2","3"]


==================================================
4. 기초 예제 (PATTERN 사고)
==================================================

--------------------------------------------------
4-1. 검사 예제 (test)
--------------------------------------------------

숫자 포함 검사:
  /\d/

문자열 시작 검사:
  /^A/

문자열 끝 검사:
  /!$/

특정 단어 포함 (대소문자 무시):
  /js/i


--------------------------------------------------
4-2. 정리 예제 (replace)
--------------------------------------------------

숫자만 남기기:
  /\D/g

공백 제거:
  /\s/g

알파벳만 남기기:
  /[^a-zA-Z]/g


==================================================
5. 중급 패턴 사고
==================================================

--------------------------------------------------
5-1. split vs replace
--------------------------------------------------

split(".")[0]
- 단순
- 파일명에 점이 여러 개면 위험

replace + 정규식
- 문자열 끝만 정확히 처리
- 실무에서 더 안전


--------------------------------------------------
5-2. 파일명 / 확장자 처리
--------------------------------------------------

확장자 제거:
  /\.(png|jpg|gif)$/

확장자 검사:
  /\.png$/


==================================================
6. 실무 예제 (REAL CASE)
==================================================

--------------------------------------------------
6-1. 전화번호 정제
--------------------------------------------------

"010-1234-5678"
→ /\D/g 제거
→ "01012345678"


--------------------------------------------------
6-2. 문자열 카운팅
--------------------------------------------------

- split("")
- filter + 정규식
- reduce로 객체 집계


--------------------------------------------------
6-3. 파일명 목록 정리
--------------------------------------------------

- replace + $
- split 방식과 비교


==================================================
7. 정규식 + JS 응용
==================================================

--------------------------------------------------
7-1. 정규식 + 배열 메서드
--------------------------------------------------

split → filter → reduce
- 정규식은 filter 조건 역할


--------------------------------------------------
7-2. reduce 그룹화 패턴
--------------------------------------------------

기본형:
  if (!acc[key]) acc[key] = [];
  acc[key].push(value);

축약형:
  (acc[key] ??= []).push(value);


==================================================
8. 정규식 사고 공식 (핵심)
==================================================

1) 문자열에 규칙이 있는가?
2) 그 규칙을 코드로 표현해야 하는가?
3) 조건문보다 정규식이 더 명확한가?
4) 남길 것인가 / 제거할 것인가?
5) 시작/끝 위치가 중요한가?


==================================================
마지막 정리
==================================================

정규식은
"외워서 쓰는 문법"이 아니라
"의도를 기호로 번역하는 사고 방식"이다




// --------------------------------------------------
// 문제 1 (⭐⭐⭐⭐) — 문자열 가공 (replace + 정규식)
// 조건:
// - phone 문자열들에서 숫자만 남기기 (하이픈/공백/괄호 제거)
// - "01012345678" 형태로 통일한 배열 출력
// 힌트: replace + 정규식 사용
// --------------------------------------------------
const phones4 = [
  "010-1234-5678",
  "010 9876 5432",
  "(010) 2222-3333",
  "010-0000-0000",
];

// 👉 코드 작성
let result4 = phones4.map(phone =>
  phone.replace(/\W/g, "")
);
// replace()란? 문자열.replace(찾을값, 바꿀값)
// "hello world".replace("world", "JS");
// "hello JS"

// /a/ → a 찾기
// /[0-9]/ → 숫자 찾기
// /\D/ → 숫자가 아닌 것 찾기

// \D 숫자
// \W 문자+숫자+_
// \S 공백
console.log(result4);
// 👉 출력값:
// ["01012345678", "01098765432", "01022223333", "01000000000"]



// --------------------------------------------------
// 문제 2 (⭐⭐⭐) — 문자열 카운팅
// 조건:
// - sentence에서
// - 각 알파벳의 등장 횟수를 객체로 만들어라
// - 대소문자 구분 X
// --------------------------------------------------
const sentence1 = "Frontend Developer";

// 👉 코드 작성
let resultA = sentence1
    .toLowerCase()
    .split("")                              // 각 알파벳의 등장 횟수 카운팅
    // .filter(char => char !== " ")       // 공백(스페이스)을 제거하는 역할
    .filter(char => /[a-z]/.test(char))
    // 알파벳(a~z)인 문자만 배열에 남겨라
    // /[a-z]/ a부터 z까지 중 하나라도 포함되면 매칭
    // .test() 문자열이 정규식 패턴과 “맞는지” 검사, 결과는 true / false
    .reduce((acc, char) => {
        acc[char] = (acc[char] || 0) + 1;
        return acc;
    }, {});

console.log(resultA);
// 👉 출력값:
// { f: 1, r: 2, o: 2, n: 2, t: 1, e: 4, d: 2, v: 1, l: 1, p: 1 }



// --------------------------------------------------
// 문제 3 (⭐⭐⭐⭐) — 문자열 패턴 변환
// 조건:
// - fileNames 배열에서
// - 확장자(.png, .jpg, .gif)를 제거
// - 파일명만 배열로 출력
// --------------------------------------------------
const fileNames4 = [
  "profile.png",
  "banner.jpg",
  "icon.gif",
  "thumbnail.png",
];

// 👉 코드 작성
let resultD = fileNames4.map(name =>
  name.replace(/\.(png|jpg|gif)$/, "")
  // 파일명 문자열에서 맨 뒤에 붙은 확장자(.png, .jpg, .gif)만 제거하라
);

let resultE = fileNames4.map(name =>
  name.split(".")[0]
  // 파일명을 점(.) 기준으로 나눠서, 첫 번째 조각만 사용하라
);

console.log(resultD);
console.log(resultE);

// 👉 출력값:
// ["profile", "banner", "icon", "thumbnail"]



// --------------------------------------------------
// 문제 4 (⭐⭐⭐⭐⭐) — 객체 그룹화
// 조건:
// - category별로 items 배열 만들기
// --------------------------------------------------
const products5 = [
  { name: "사과", category: "fruit" },
  { name: "감자", category: "vegi" },
  { name: "딸기", category: "fruit" },
  { name: "양파", category: "vegi" },
  { name: "키위", category: "fruit" },
];

// 👉 코드 작성
let result = products5.reduce((acc, cur) => {
    if(!acc[cur.category]) {
        acc[cur.category] = [];
    }
    // 이 카테고리 이름을 key로 하는 배열이 아직 없으면, 새로 만들어라
    acc[cur.category].push(cur.name);
    return acc;
    // 카테고리(key)별로 만들어 둔 배열에, 해당 항목의 이름(name)을 하나씩 추가하면서 
    // 최종적으로 하나의 객체(acc)를 완성해라
}, {});

let result5 = products5.reduce((acc, cur) => {
  (acc[cur.category] ??= []).push(cur.name);
  return acc;
  // 현재 항목의 category를 key로 하는 배열이 아직 없으면 빈 배열을 만들고,
  // 그 배열에 현재 항목의 name을 하나 추가하라.
}, {});

console.log(result);
console.log(result5);
// 👉 출력값:
// {
//   fruit: ["사과", "딸기", "키위"],
//   vegi: ["감자", "양파"]
// }
