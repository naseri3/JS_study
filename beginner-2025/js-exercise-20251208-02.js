// ===============================================
// 📌 12/8 — 업그레이드 JS 문제 (개선 로직 중심)
// ===============================================


// -----------------------------------------------
// 문제 1 — filter + map 조합 ⭐⭐⭐⭐
// -----------------------------------------------
//
// products 배열에서
// * 재고(stock) 3개 이상
// * 가격(price) 15,000원 이하
// 인 상품의 이름만 배열로 반환하세요.
//
// (힌트: filter → map 체이닝)

const products2 = [  { name: "키위", price: 9000, stock: 2 },
  { name: "복숭아", price: 11000, stock: 6 },
  { name: "딸기", price: 13000, stock: 3 },
  { name: "샤인머스캣", price: 22000, stock: 10 },
  { name: "블루베리", price: 15000, stock: 8 },
];

// 👉 코드 작성
const result = products2
  .filter(item => item.stock >= 3 && item.price <= 15000)
  .map(item => item.name);


console.log("문제1 결과:", result);
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 2 — 문자열 + 배열 + map ⭐⭐⭐⭐⭐
// -----------------------------------------------  
//
// message 문자열에서 단어 길이를 숫자로 담은 배열을 만드세요.
//
// 예: "Hello JS World"
// → [5, 2, 5]
//
// 단, *for / forEach 사용 금지* (map 필수)

const message = "Where UX meets Frontend logic";

// 👉 코드 작성
const words = message.split(" ");
const result1 = words.map(word => {
    return word.length;
})

console.log("문제2 결과:", result1);
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 3 — reduce로 평균 구하기 ⭐⭐⭐⭐⭐
// -----------------------------------------------
//
// reduce를 사용해 80점 이상 점수들의 평균을 구하세요.
// (toFixed(2)로 소수점 2자리 유지)
//
// filter 사용 가능.

const scores2 = [95, 52, 88, 100, 67, 82];

// 👉 코드 작성
const filtered = scores2.filter(score => score >= 80);
const avg = filtered.reduce((acc, cur) => {
    return acc + cur;
},0) / filtered.length;

console.log("문제3 결과:", avg.toFixed(2)); 
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 4 — Object.keys / map 변환 ⭐⭐⭐⭐
// -----------------------------------------------
//
// 아래 객체를 다음과 같은 형태로 변환하세요.
//
// 입력:
// { name: "유재석", age: 50, job: "MC" }
//
// 출력:
// ["name → 유재석", "age → 50", "job → MC"]
//
// (힌트: Object.keys + map 조합)

const member = {
  name: "유재석",
  age: 50,
  job: "MC"
};

// 👉 코드 작성
const keys = Object.keys(member);
const result2 = keys.map(key => {
    return `${key} -> ${member[key]}`;
})

console.log("문제4 결과:", result2);
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 5 — 중복 제거 + 정렬 + map ⭐⭐⭐⭐⭐
//
// tags2 배열에서 중복을 제거하고,
// 모든 텍스트를 대문자로 변환 후,
// 알파벳 순으로 정렬하여 출력하세요.
//
// (힌트: Set 사용 가능, 그러나 최종 결과는 map/sort 활용)

const tags2 = ["css", "html", "js", "css", "react", "html", "node", "js"];

// 👉 코드 작성
const noDup = [...new Set(tags2)];
const formatted = noDup
    .map(tag => tag.toUpperCase())
    .sort();

console.log("문제5 결과:", formatted);
console.log("---------------------------------------");




// ===============================================
// ✨ 다 풀면: "체크해줘 (개선버전)" 라고 말하기
// ===============================================
