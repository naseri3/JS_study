// ===============================================
// 📌 12/8 JavaScript Practice — 난이도 ⭐⭐⭐~⭐⭐⭐⭐
// ===============================================


// -----------------------------------------------
// 문제 1 — for 사용 (조건 필터링)
// -----------------------------------------------

// products 배열에서 재고(stock)이 5개 이상이고,
// 가격(price)이 10,000원 이하인 상품 이름만 newList 배열에 push 하세요.

const products = [
  { name: "사과", price: 8000, stock: 3 },
  { name: "바나나", price: 6000, stock: 12 },
  { name: "참외", price: 12000, stock: 10 },
  { name: "수박", price: 20000, stock: 5 },
  { name: "오렌지", price: 9000, stock: 7 },
];

let newList = [];

// 👉 코드 작성
for(let i = 0; i < products.length; i++) {
    if(products[i].stock >= 5 && products[i].price <= 10000) {
        newList.push(products[i].name);
    }
}
console.log("문제1 결과:", newList);
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 2 — for...of (문자열 처리)
// -----------------------------------------------

// sentence 문자열에서 공백을 기준으로 단어를 분리하고,
// 글자 수가 4글자 이하인 단어만 출력하세요.

const sentence = "JavaScript allows flexible way to solve problems";

// 👉 코드 작성
const words = sentence.split(" ");          // sentence 하나씩 꺼내기
for (const word of words) {
    if(word.length <= 4) {
        console.log(word);
    }
}


console.log("---------------------------------------");



// -----------------------------------------------
// 문제 3 — forEach + 조건 + 평균 구하기
// (filter / reduce 사용 금지)
// -----------------------------------------------

const scores = [55, 78, 92, 67, 88, 45, 73];

// 👉 조건: 70점 이상만 모아서 평균 계산
// 👉 코드 작성
let sum = 0;
scores.forEach(num => {
    if (num >= 70) {
        sum += num;
    }
});

let avg = sum / scores.length;
console.log("문제3 결과:", avg.toFixed(2));

console.log("---------------------------------------");



// -----------------------------------------------
// 문제 4 — for...in (객체 → 배열 변환)
// -----------------------------------------------

// userInfo 객체를 "key : value" 문자열 배열로 변환

const userInfo = {
  name: "이서연",
  job: "Frontend Developer",
  level: "Junior",
};

let resultArr = [];

// 👉 코드 작성
for (const key in userInfo) {
    resultArr.push(`${key} : ${userInfo[key]}`);
}

console.log("문제4 결과:", resultArr);
console.log("---------------------------------------");



// -----------------------------------------------
// 문제 5 — 난이도 상승: 중복 제거 로직
// (Set 사용 ❌)
// -----------------------------------------------

const tags = ["css", "html", "js", "css", "js", "react", "html"];

let uniqueTags = [];

// 👉 includes() 활용해서 중복 없게 push
// 👉 코드 작성
for (const tag of tags) {
    if(!uniqueTags.includes(tag)) {
        uniqueTags.push(tag);
    }
}

console.log("문제5 결과:", uniqueTags);
console.log("---------------------------------------");




// ===============================================
// ✨ 문제 풀이 완료하면: "체크해줘" 라고 말하기
// ===============================================
